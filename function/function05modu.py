"""
module are 2 types ->1.built in module
                    ->2.external module    
"""

import math
import os
import mymodule
import requests  #requests.py save in python it exist but not see
#a = int(input("enter a number"))
#print(math.sqrt(a))
os.abort
mymodule.hello()
r = requests.get("https://www.google.com")
print(r.text)